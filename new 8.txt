CREATE TABLE `poctdm`.`datamaked` (
  `emp_id` INT NULL, given df['emp_id']=np.arange(len(df)) since no id value in faker
  `emp_name` VARCHAR(45) NOT NULL,
  `emp_address` VARCHAR(45) NULL, length increased to 500
  `emp_dob` DATE NULL,
  `credit_card_number` INT NULL, changed to double as length is not handling by int
  `emp_ssn` INT NULL); changed to varchar as special charaters are there in that



CREATE TABLE `poctdm`.`masked_data` (
  `emp_id` INT NOT NULL,
  `emp_name` VARCHAR(45) NULL,
  `emp_address` VARCHAR(500) NULL,
  `emp_dob` DATE NULL,
  `credit_card_number` DOUBLE NULL,
  `emp_ssn` VARCHAR(45) NULL,
  PRIMARY KEY (`emp_id`));


from pandas.io import sql
import MySQLdb

con = MySQLdb.connect()
sql.write_frame(df, con=con, name='table_name_for_df', 
                if_exists='replace', flavor='mysql')